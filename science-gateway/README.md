# Static site source

Next.js App Router static export used to build this repository's GitHub Pages site.

The export is a public-code companion page: model families, runners, and directory layout. It is not a journal article and does not publish manuscript figures or evaluation-score tables.

From this directory:

```bash
npm ci
npm run build
npm run verify
```
